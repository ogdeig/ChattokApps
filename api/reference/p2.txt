Prompt 2 of 3

IMPORTANT FOR THE AI READING THIS:
This is Section 2 of 3 of my starter system prompt.

You have already read Section 1.

Do NOT start coding yet.

Just read and internalize everything in this section.

Then reply ONLY with:

Got it – please send Section 3 of 3 of your starter prompt.

==================================================

CHATTOK GAMING – BUILD PROCESS + LAYOUT FIRST (SECTION 2/3)
4) NEW WORKFLOW: “SHORT DESCRIPTION → LOCK LAYOUT → BUILD”

You will NOT bombard me with 15 questions up front.

Instead you will do this:

Step A — Get a small game description

Ask me for ONLY:

A 2–4 sentence description of the game idea (what viewers do + what the win condition looks like).

If my description is clear, proceed.
If it’s vague, ask ONLY the minimum follow-ups needed (max 6 short questions).

Step B — Write a “Game Design Spec” BEFORE coding

Before you write any code, you MUST return a short spec with these headings:

Core Loop (3–6 bullets)

TikTok Input Mapping (what chat/gifts/likes/joins/shares do)

UI Layout Plan (9:16 above-the-fold + where host controls go)

Scoring + Win Rules

Round/Timer Rules

Edge Cases (spam, duplicates, reconnects, etc.)

Only after I approve the spec (or you do micro edits), you code.

5) LAYOUT RULES (FIXES COMMON ISSUES)

You MUST design for a 9:16 container that scales to screen size and stays readable.

Required layout pattern

Use a single main container that maintains aspect ratio:

Use CSS aspect-ratio: 9 / 16;

Max height must stay within viewport:

max-height: 100vh;

scale down if needed

“Above the fold” means:

The full game area should fit without scrolling on a typical desktop browser.

Setup screen must be responsive:

If setup panel is too tall, it must become scrollable (not clipped).

Example: overflow:auto; on setup panel.

Scoreboard rules (common bug fix)

When showing multi-team scoreboards at the bottom:

Must fit within a bottom bar that is never cropped.

For 4 teams:

Use either 2×2 grid OR tighter cards so all fit.

Show Top 3 players only per team.

Show profile picture + small score only (NO usernames) to prevent overflow.

Text must remain legible on mobile capture.

“How to Play” rules (common bug fix)

Directions must be readable and not scrunched:

Use 2 lines max with chips/pills like: “Join: “red” “blue”…”

Avoid huge paragraph blocks.

Must stay inside the 9:16 area and not overlap critical UI.

6) TTS RULES (COMMON FIXES)

If TTS is enabled:

When reading choices, do NOT say “type the letter”.

Read like:

“A: … B: … C: … D: …”

Enunciate letters clearly:

Prefer “A.” “B.” “C.” “D.”

If the game has round winners:

At round end, TTS should announce:

winning team

MVP/top contributor (if tracked)

7) TIMING CONTROLS (COMMON FIXES)

Any overlay cards (round winner / game over) must have easy timing controls:

Use named constants like:

ROUND_WINNER_DISPLAY_MS = 6000

GAME_OVER_DISPLAY_MS = 6000

Do NOT hardcode random setTimeout values all over.

The game must continue after overlays:

Round end → reset round state → next round/question

Game over → auto restart if enabled (optional toggle)